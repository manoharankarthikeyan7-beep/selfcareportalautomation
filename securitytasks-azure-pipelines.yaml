trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: AppSecrets 
  - name: webAppName
    value: 'PipelineAPI' 
  - name: azureServiceConnection
    value: 'Azure subscription 1 (e26f51cc-2002-4eef-9131-5841b18763a7)' 
  - name: resourceGroupName
    value: 'pipelineautomationapi'
  
  # SonarCloud Identifiers
  - name: sonarCloudOrg
    value: 'senthiln0281'
  - name: sonarProjectKey
    value: 'senthiln0281_Pipeline'

stages:
- stage: Security_and_Build
  displayName: 'Scan and Build'
  jobs:
  - job: Secure_Build
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '18.x'

    # --- SECTION 1: SONARCLOUD PREPARATION ---
    # This task MUST come before any build/install steps.
    - task: SonarCloudPrepare@3
      displayName: 'Prepare SonarCloud'
      inputs:
        SonarCloud: 'SonarCloudConnection'
        organization: '$(sonarCloudOrg)'
        scannerMode: 'CLI'
        configMode: 'manual'
        cliProjectKey: '$(sonarProjectKey)'
        cliProjectName: 'PipelineGenerator'
        cliSources: '.' # Tells SonarCloud to scan everything from the root

    - script: |
        cd backend
        npm install
      displayName: 'Install Backend'

    - script: |
        cd frontend
        npm install
        npm run build
      displayName: 'Build Frontend'

    # --- SECTION 2: SNYK VULNERABILITY SCAN ---
    # FIX: Added --all-projects to find package.json in subdirectories.
    - task: SnykSecurityScan@1
      displayName: 'Snyk Security Scan'
      inputs:
        serviceConnectionEndpoint: 'SnykConnection'
        testType: 'app'
        monitorOnBuild: true
        failOnIssues: true
        organization: '262c0b5b-633f-4261-a4b6-762bc547c1e9'
        additionalArguments: '--all-projects'

    # --- SECTION 3: SONARCLOUD EXECUTION & PUSH ---
    # These two tasks actually push the report to SonarCloud.
    - task: SonarCloudAnalyze@3
      displayName: 'Run SonarCloud Analysis'

    - task: SonarCloudPublish@3
      displayName: 'Publish Quality Gate Result'
      inputs:
        pollingTimeoutSec: '300'

    # --- SECTION 4: ARCHIVE & PUBLISH ARTIFACTS ---
    - script: |
        mkdir -p backend/public
        cp -r frontend/build/* backend/public/
      displayName: 'Merge Frontend to Backend'

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: 'backend'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'

    - publish: $(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip
      artifact: drop

- stage: Deploy
  displayName: 'Deploy'
  dependsOn: Security_and_Build
  condition: succeeded() 
  jobs:
  - job: DeployWeb
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - download: current
      artifact: drop
    - task: AzureWebApp@1
      inputs:
        azureSubscription: $(azureServiceConnection)
        appType: 'webAppLinux'
        appName: $(webAppName)
        package: '$(Pipeline.Workspace)/drop/$(Build.BuildId).zip'
        runtimeStack: 'NODE|18-lts'